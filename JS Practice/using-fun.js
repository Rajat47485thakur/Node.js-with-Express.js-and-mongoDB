function add(x, y) {
    console.log(`If we ${x} + ${y} = ${x + y}`)
}
add(4, 9);


function subtraction(x, y) {
    console.log(`If we ${x} - ${y} = ${x - y}`)
}
subtraction(4, 9);

function multiply(x, y) {
    console.log(`If we ${x} * ${y} = ${x * y}`)
}
multiply(4, 9);

function divide(x, y) {
    console.log(`If we ${x} / ${y} = ${x / y}`)
}
divide(4, 9);